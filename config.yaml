# =============================================================================
# Previs Raspberry Client — Configuration
# =============================================================================
# Edit this file to point the client at your Pexip Infinity deployment.
# After editing, restart the service:
#
#   sudo systemctl restart previs-client
#
# Keys
# ----
# server        Hostname (or IP) of your Pexip Infinity Conferencing Node
#               e.g. "vc.example.com" or "192.168.1.100"
#
# vmr           The Virtual Meeting Room alias to dial into
#               e.g. "meet.boardroom" or "vmr@vc.example.com"
#
# display_name  The name that appears for this device in the participant list
#
# pin           Host or guest PIN for the VMR (leave empty if none required)
#
# reconnect_delay_seconds
#               How long to wait before retrying after a call drops.
#               Minimum 5 seconds.
#
# camera        Optional. Part of the name of the capture device to use, e.g.
# microphone    "USB Camera" or "USB Audio". Matching is case insensitive and
# speaker       matches anywhere in the device name; the client logs every
#               device it finds at start-up, so run
#               'journalctl -u previs-client' to see the exact names.
#               Leave empty to auto-detect: the client then picks the system
#               default when it is usable and otherwise the first device that
#               is not a PulseAudio monitor source or one of the Raspberry Pi's
#               internal video nodes (bcm2835-isp, pispbe, ...).
# =============================================================================

server: "vc.example.com"
vmr: "meet.boardroom"
display_name: "Raspberry Pi Client"
pin: ""
reconnect_delay_seconds: 10
camera: ""
microphone: ""
speaker: ""
